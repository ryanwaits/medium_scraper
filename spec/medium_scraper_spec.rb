require "spec_helper"

describe MediumScraper do
  it "has a version number" do
    expect(MediumScraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
